---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/pages/shop.css';
---

<!DOCTYPE html>
<html lang="en">

<head>
    <BaseHead title="Shop - T.S.K.V. Spartacus" description="Official T.S.K.V. Spartacus merchandise. Get your Spartacus gear today!" />
</head>

<body>
    <Header />

        <!-- Hero Section -->
        <section class="hero">
            <div class="container">
                <h1>Spartacus Shop</h1>
                <h2>Official Merchandise</h2>
            </div>
        </section>

        <!-- Status Banner (shown after checkout) -->
        <div id="status-banner" class="status-banner hidden"></div>

        <!-- Floating Cart Button -->
        <button id="cart-toggle" class="cart-floating-btn hidden" aria-label="Open cart">
            <i class="fas fa-shopping-cart"></i>
            <span id="cart-badge" class="cart-badge">0</span>
        </button>

        <!-- Cart Sidebar -->
        <div id="cart-overlay" class="cart-overlay hidden"></div>
        <aside id="cart-sidebar" class="cart-sidebar">
            <div class="cart-header">
                <h3><i class="fas fa-shopping-cart"></i> Your Cart</h3>
                <button id="cart-close" class="cart-close" aria-label="Close cart">&times;</button>
            </div>
            <div id="cart-items" class="cart-items">
                <p class="cart-empty">Your cart is empty.</p>
            </div>
            <div class="cart-footer">
                <div class="cart-total">
                    <span>Total:</span>
                    <span id="cart-total-price">&euro;0.00</span>
                </div>
                <button id="checkout-btn" class="btn checkout-btn" disabled>
                    <i class="fas fa-lock"></i> Checkout
                </button>
            </div>
        </aside>

        <!-- Main Content -->
        <main>
            <div class="container">
                <section class="section">
                    <h2 class="section-title centered-title">Our Merchandise</h2>
                    <p class="shop-intro text-center">Rep Spartacus at the gym and beyond. Select your colour and place your order below.</p>

                    <div class="product-grid">

                        <!-- Classic Powerlifting Shirt -->
                        <div class="product-card" data-base-id="classic-powerlifting-shirt">
                            <div class="product-image-wrap">
                                <img src="/images/Classic%20Powerlifting%20shirt_Black.jpg"
                                     alt="Classic Powerlifting Shirt"
                                     class="product-img">
                            </div>
                            <div class="product-info">
                                <h3>Classic Powerlifting Shirt</h3>
                                <p class="product-description">Official T.S.K.V. Spartacus powerlifting shirt.</p>
                                <div class="color-selector">
                                    <span class="color-label">Color: <strong class="selected-color-name">Black</strong></span>
                                    <div class="color-swatches">
                                        <button class="color-swatch active" data-color="black" data-color-name="Black" data-img="/images/Classic%20Powerlifting%20shirt_Black.jpg" style="background:#1c1c1c" aria-label="Black"></button>
                                        <button class="color-swatch" data-color="red" data-color-name="Red" data-img="/images/Classic%20Powerlifting%20shirt_Red.jpg" style="background:#cc1111" aria-label="Red"></button>
                                        <button class="color-swatch white-swatch" data-color="white" data-color-name="White" data-img="/images/Classic%20Powerlifting%20shirt_White.jpg" style="background:#ffffff" aria-label="White"></button>
                                    </div>
                                </div>
                                <div class="size-selector">
                                    <label class="size-label">Fit:</label>
                                    <select class="fit-select" aria-label="Select fit">
                                        <option value="">Select fit</option>
                                        <option value="Men's">Men's</option>
                                        <option value="Women's">Women's</option>
                                    </select>
                                </div>
                                <div class="size-selector">
                                    <label class="size-label">Size:</label>
                                    <select class="size-select" aria-label="Select size">
                                        <option value="">Select size</option>
                                        <option value="XS">XS</option>
                                        <option value="S">S</option>
                                        <option value="M">M</option>
                                        <option value="L">L</option>
                                        <option value="XL">XL</option>
                                        <option value="XXL">XXL</option>
                                    </select>
                                </div>
                                <div class="product-price">&euro;27.50</div>
                                <button class="btn add-to-cart-btn" data-product-name="Classic Powerlifting Shirt" data-product-price="27.50">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>

                        <!-- Competition Shirt -->
                        <div class="product-card" data-base-id="competition-shirt">
                            <div class="product-image-wrap">
                                <img src="/images/Competition%20shirt_black.jpg"
                                     alt="Competition Shirt"
                                     class="product-img">
                            </div>
                            <div class="product-info">
                                <h3>Competition Shirt</h3>
                                <p class="product-description">Official T.S.K.V. Spartacus competition shirt.</p>
                                <div class="color-selector">
                                    <span class="color-label">Color: <strong class="selected-color-name">Black</strong></span>
                                    <div class="color-swatches">
                                        <button class="color-swatch active" data-color="black" data-color-name="Black" data-img="/images/Competition%20shirt_black.jpg" style="background:#1c1c1c" aria-label="Black"></button>
                                        <button class="color-swatch" data-color="red" data-color-name="Red" data-img="/images/Competition%20shirt_red.jpg" style="background:#cc1111" aria-label="Red"></button>
                                        <button class="color-swatch white-swatch" data-color="white" data-color-name="White" data-img="/images/Competition%20shirt_white.jpg" style="background:#ffffff" aria-label="White"></button>
                                    </div>
                                </div>
                                <div class="size-selector">
                                    <label class="size-label">Fit:</label>
                                    <select class="fit-select" aria-label="Select fit">
                                        <option value="">Select fit</option>
                                        <option value="Men's">Men's</option>
                                        <option value="Women's">Women's</option>
                                    </select>
                                </div>
                                <div class="size-selector">
                                    <label class="size-label">Size:</label>
                                    <select class="size-select" aria-label="Select size">
                                        <option value="">Select size</option>
                                        <option value="XS">XS</option>
                                        <option value="S">S</option>
                                        <option value="M">M</option>
                                        <option value="L">L</option>
                                        <option value="XL">XL</option>
                                        <option value="XXL">XXL</option>
                                    </select>
                                </div>
                                <div class="product-price">&euro;27.50</div>
                                <button class="btn add-to-cart-btn" data-product-name="Competition Shirt" data-product-price="27.50">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>

                        <!-- Hoodie with Zip -->
                        <div class="product-card" data-base-id="hoodie-zip">
                            <div class="product-image-wrap has-slider">
                                <img src="/images/Hoodie%20with%20Zip_black%20front.jpg" alt="Hoodie with Zip front" class="product-img slide-img active">
                                <img src="/images/Hoodie%20with%20Zip_black%20back.jpg" alt="Hoodie with Zip back" class="product-img slide-img">
                                <button class="slide-btn prev" aria-label="Previous image">&#8249;</button>
                                <button class="slide-btn next" aria-label="Next image">&#8250;</button>
                                <div class="slide-dots">
                                    <button class="dot active" data-idx="0" aria-label="Front view"></button>
                                    <button class="dot" data-idx="1" aria-label="Back view"></button>
                                </div>
                            </div>
                            <div class="product-info">
                                <h3>Hoodie with Zip</h3>
                                <p class="product-description">Official T.S.K.V. Spartacus zip hoodie.</p>
                                <div class="color-selector">
                                    <span class="color-label">Color: <strong class="selected-color-name">Black</strong></span>
                                    <div class="color-swatches">
                                        <button class="color-swatch active" data-color="black" data-color-name="Black" data-img-front="/images/Hoodie%20with%20Zip_black%20front.jpg" data-img-back="/images/Hoodie%20with%20Zip_black%20back.jpg" style="background:#1c1c1c" aria-label="Black"></button>
                                        <button class="color-swatch" data-color="burgundy" data-color-name="Burgundy" data-img-front="/images/Hoodie%20with%20Zip_burgunday%20front.jpg" data-img-back="/images/Hoodie%20with%20Zip_burgundy%20back.jpg" style="background:#6b0f1a" aria-label="Burgundy"></button>
                                    </div>
                                </div>
                                <div class="size-selector">
                                    <label class="size-label">Size:</label>
                                    <select class="size-select" aria-label="Select size">
                                        <option value="">Select size</option>
                                        <option value="XS">XS</option>
                                        <option value="S">S</option>
                                        <option value="M">M</option>
                                        <option value="L">L</option>
                                        <option value="XL">XL</option>
                                        <option value="XXL">XXL</option>
                                    </select>
                                </div>
                                <div class="product-price">&euro;50.00</div>
                                <button class="btn add-to-cart-btn" data-product-name="Hoodie with Zip" data-product-price="50.00">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>

                        <!-- Joggers -->
                        <div class="product-card" data-base-id="joggers">
                            <div class="product-image-wrap has-slider">
                                <img src="/images/Joggers_red%20front.jpg" alt="Joggers front" class="product-img slide-img active">
                                <img src="/images/Joggers_red%20back.jpg" alt="Joggers back" class="product-img slide-img">
                                <button class="slide-btn prev" aria-label="Previous image">&#8249;</button>
                                <button class="slide-btn next" aria-label="Next image">&#8250;</button>
                                <div class="slide-dots">
                                    <button class="dot active" data-idx="0" aria-label="Front view"></button>
                                    <button class="dot" data-idx="1" aria-label="Back view"></button>
                                </div>
                            </div>
                            <div class="product-info">
                                <h3>Joggers</h3>
                                <p class="product-description">Official T.S.K.V. Spartacus joggers.</p>
                                <div class="color-selector">
                                    <span class="color-label">Color: <strong class="selected-color-name">Red</strong></span>
                                    <div class="color-swatches">
                                        <button class="color-swatch active" data-color="red" data-color-name="Red" data-img-front="/images/Joggers_red%20front.jpg" data-img-back="/images/Joggers_red%20back.jpg" style="background:#cc1111" aria-label="Red"></button>
                                        <button class="color-swatch white-swatch" data-color="white" data-color-name="White" data-img-front="/images/Joggers_white%20front.jpg" data-img-back="/images/Joggers_white%20back.jpg" style="background:#ffffff" aria-label="White"></button>
                                    </div>
                                </div>
                                <div class="size-selector">
                                    <label class="size-label">Size:</label>
                                    <select class="size-select" aria-label="Select size">
                                        <option value="">Select size</option>
                                        <option value="XS">XS</option>
                                        <option value="S">S</option>
                                        <option value="M">M</option>
                                        <option value="L">L</option>
                                        <option value="XL">XL</option>
                                        <option value="XXL">XXL</option>
                                    </select>
                                </div>
                                <div class="product-price">&euro;35.00</div>
                                <button class="btn add-to-cart-btn" data-product-name="Joggers" data-product-price="35.00">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>

                        <!-- Shorts -->
                        <div class="product-card" data-base-id="shorts">
                            <div class="product-image-wrap has-slider">
                                <img src="/images/Shorts_front.jpg" alt="Shorts front" class="product-img slide-img active">
                                <img src="/images/Shorts_back.jpg" alt="Shorts back" class="product-img slide-img">
                                <button class="slide-btn prev" aria-label="Previous image">&#8249;</button>
                                <button class="slide-btn next" aria-label="Next image">&#8250;</button>
                                <div class="slide-dots">
                                    <button class="dot active" data-idx="0" aria-label="Front view"></button>
                                    <button class="dot" data-idx="1" aria-label="Back view"></button>
                                </div>
                            </div>
                            <div class="product-info">
                                <h3>Shorts</h3>
                                <p class="product-description">Official T.S.K.V. Spartacus shorts.</p>
                                <div class="size-selector">
                                    <label class="size-label">Size:</label>
                                    <select class="size-select" aria-label="Select size">
                                        <option value="">Select size</option>
                                        <option value="XS">XS</option>
                                        <option value="S">S</option>
                                        <option value="M">M</option>
                                        <option value="L">L</option>
                                        <option value="XL">XL</option>
                                        <option value="XXL">XXL</option>
                                    </select>
                                </div>
                                <div class="product-price">&euro;30.00</div>
                                <button class="btn add-to-cart-btn" data-product-id="shorts-black" data-product-name="Shorts (Black)" data-product-price="30.00">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>

                        <!-- Tank Top (Men) -->
                        <div class="product-card" data-base-id="tanktop-men">
                            <div class="product-image-wrap">
                                <img src="/images/Tanktop%20men_black.jpg"
                                     alt="Tank Top Men"
                                     class="product-img">
                            </div>
                            <div class="product-info">
                                <h3>Tank Top (Men)</h3>
                                <p class="product-description">Official T.S.K.V. Spartacus men's tank top.</p>
                                <div class="color-selector">
                                    <span class="color-label">Color: <strong class="selected-color-name">Black</strong></span>
                                    <div class="color-swatches">
                                        <button class="color-swatch active" data-color="black" data-color-name="Black" data-img="/images/Tanktop%20men_black.jpg" style="background:#1c1c1c" aria-label="Black"></button>
                                        <button class="color-swatch" data-color="red" data-color-name="Red" data-img="/images/Tanktop%20men_red.jpg" style="background:#cc1111" aria-label="Red"></button>
                                        <button class="color-swatch white-swatch" data-color="white" data-color-name="White" data-img="/images/Tanktop%20men_white.jpg" style="background:#ffffff" aria-label="White"></button>
                                    </div>
                                </div>
                                <div class="size-selector">
                                    <label class="size-label">Size:</label>
                                    <select class="size-select" aria-label="Select size">
                                        <option value="">Select size</option>
                                        <option value="XS">XS</option>
                                        <option value="S">S</option>
                                        <option value="M">M</option>
                                        <option value="L">L</option>
                                        <option value="XL">XL</option>
                                        <option value="XXL">XXL</option>
                                    </select>
                                </div>
                                <div class="product-price">&euro;22.50</div>
                                <button class="btn add-to-cart-btn" data-product-name="Tank Top (Men)" data-product-price="22.50">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>

                        <!-- Tank Top (Women) -->
                        <div class="product-card" data-base-id="tanktop-women">
                            <div class="product-image-wrap">
                                <img src="/images/Tanktop%20women_black.jpg"
                                     alt="Tank Top Women"
                                     class="product-img">
                            </div>
                            <div class="product-info">
                                <h3>Tank Top (Women)</h3>
                                <p class="product-description">Official T.S.K.V. Spartacus women's tank top.</p>
                                <div class="color-selector">
                                    <span class="color-label">Color: <strong class="selected-color-name">Black</strong></span>
                                    <div class="color-swatches">
                                        <button class="color-swatch active" data-color="black" data-color-name="Black" data-img="/images/Tanktop%20women_black.jpg" style="background:#1c1c1c" aria-label="Black"></button>
                                        <button class="color-swatch" data-color="red" data-color-name="Red" data-img="/images/Tanktop%20women_red.jpg" style="background:#cc1111" aria-label="Red"></button>
                                        <button class="color-swatch white-swatch" data-color="white" data-color-name="White" data-img="/images/Tanktop%20women_white.jpg" style="background:#ffffff" aria-label="White"></button>
                                    </div>
                                </div>
                                <div class="size-selector">
                                    <label class="size-label">Size:</label>
                                    <select class="size-select" aria-label="Select size">
                                        <option value="">Select size</option>
                                        <option value="XS">XS</option>
                                        <option value="S">S</option>
                                        <option value="M">M</option>
                                        <option value="L">L</option>
                                        <option value="XL">XL</option>
                                        <option value="XXL">XXL</option>
                                    </select>
                                </div>
                                <div class="product-price">&euro;22.50</div>
                                <button class="btn add-to-cart-btn" data-product-name="Tank Top (Women)" data-product-price="22.50">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>

                        <!-- Varsity Jacket -->
                        <div class="product-card" data-base-id="varsity-jacket">
                            <div class="product-image-wrap has-slider">
                                <img src="/images/Varsity%20Jacket_front.jpg" alt="Varsity Jacket front" class="product-img slide-img active">
                                <img src="/images/Varsity%20Jacket_back.jpg" alt="Varsity Jacket back" class="product-img slide-img">
                                <button class="slide-btn prev" aria-label="Previous image">&#8249;</button>
                                <button class="slide-btn next" aria-label="Next image">&#8250;</button>
                                <div class="slide-dots">
                                    <button class="dot active" data-idx="0" aria-label="Front view"></button>
                                    <button class="dot" data-idx="1" aria-label="Back view"></button>
                                </div>
                            </div>
                            <div class="product-info">
                                <h3>Varsity Jacket</h3>
                                <p class="product-description">Official T.S.K.V. Spartacus varsity jacket.</p>
                                <div class="size-selector">
                                    <label class="size-label">Size:</label>
                                    <select class="size-select" aria-label="Select size">
                                        <option value="">Select size</option>
                                        <option value="XS">XS</option>
                                        <option value="S">S</option>
                                        <option value="M">M</option>
                                        <option value="L">L</option>
                                        <option value="XL">XL</option>
                                        <option value="XXL">XXL</option>
                                    </select>
                                </div>
                                <div class="product-price">&euro;50.00</div>
                                <button class="btn add-to-cart-btn" data-product-id="varsity-jacket" data-product-name="Varsity Jacket" data-product-price="50.00">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </div>
                        </div>

                        <!-- Powerlifting Team Shirt (exclusive) -->
                        <div class="product-card product-card--exclusive">
                            <div class="product-image-wrap has-slider">
                                <img src="/images/powerlifting%20team%20shirt%20front.jpg" alt="Powerlifting Team Shirt front" class="product-img slide-img active">
                                <img src="/images/powerlifting%20team%20shirt%20back.jpg" alt="Powerlifting Team Shirt back" class="product-img slide-img">
                                <button class="slide-btn prev" aria-label="Previous image">&#8249;</button>
                                <button class="slide-btn next" aria-label="Next image">&#8250;</button>
                                <div class="slide-dots">
                                    <button class="dot active" data-idx="0" aria-label="Front view"></button>
                                    <button class="dot" data-idx="1" aria-label="Back view"></button>
                                </div>
                            </div>
                            <div class="product-info">
                                <h3>Powerlifting Team Shirt</h3>
                                <p class="product-description">Exclusive to T.S.K.V. Spartacus Powerlifting Team members. This item is not available for general purchase — orders are managed directly by the board.</p>
                                <div class="exclusive-badge"><i class="fas fa-shield-halved"></i> Team Members Only</div>
                                <div class="product-price">&euro;27.50</div>
                            </div>
                        </div>

                    </div>
                </section>

                <!-- Info Section -->
                <section class="section text-center">
                    <h2 class="section-title centered-title">How It Works</h2>
                    <div class="shop-steps">
                        <div class="shop-step">
                            <div class="step-icon"><i class="fas fa-cart-plus"></i></div>
                            <h3>1. Add to Cart</h3>
                            <p>Browse our merchandise and add your favourites to the cart.</p>
                        </div>
                        <div class="shop-step">
                            <div class="step-icon"><i class="fas fa-credit-card"></i></div>
                            <h3>2. Checkout</h3>
                            <p>Securely pay with card or iDEAL through Stripe checkout.</p>
                        </div>
                        <div class="shop-step">
                            <div class="step-icon"><i class="fas fa-box-open"></i></div>
                            <h3>3. Collect</h3>
                            <p>Pick up your items at the next Spartacus training or event.</p>
                        </div>
                    </div>
                </section>
            </div>
        </main>

        <Footer />

    <script is:inline>
        document.addEventListener('DOMContentLoaded', () => {
            // ========== SHOPPING CART ==========
            let cart = [];

            const cartToggle = document.getElementById('cart-toggle');
            const cartSidebar = document.getElementById('cart-sidebar');
            const cartOverlay = document.getElementById('cart-overlay');
            const cartClose = document.getElementById('cart-close');
            const cartItemsEl = document.getElementById('cart-items');
            const cartBadge = document.getElementById('cart-badge');
            const cartTotalPrice = document.getElementById('cart-total-price');
            const checkoutBtn = document.getElementById('checkout-btn');

            function openCart() {
                cartSidebar.classList.add('open');
                cartOverlay.classList.remove('hidden');
            }

            function closeCart() {
                cartSidebar.classList.remove('open');
                cartOverlay.classList.add('hidden');
            }

            cartToggle.addEventListener('click', openCart);
            cartClose.addEventListener('click', closeCart);
            cartOverlay.addEventListener('click', closeCart);

            function updateCartUI() {
                const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
                const totalPrice = cart.reduce((sum, item) => sum + (item.quantity * parseFloat(item.price)), 0);

                // Badge
                cartBadge.textContent = totalItems;
                if (totalItems > 0) {
                    cartToggle.classList.remove('hidden');
                    checkoutBtn.disabled = false;
                } else {
                    cartToggle.classList.add('hidden');
                    checkoutBtn.disabled = true;
                }

                // Total
                cartTotalPrice.textContent = '\u20AC' + totalPrice.toFixed(2);

                // Items list
                if (cart.length === 0) {
                    cartItemsEl.innerHTML = '<p class="cart-empty">Your cart is empty.</p>';
                    return;
                }

                cartItemsEl.innerHTML = cart.map(item => `
                    <div class="cart-item">
                        <div class="cart-item-info">
                            <span class="cart-item-name">${item.name}</span>
                            <span class="cart-item-price">\u20AC${(item.quantity * parseFloat(item.price)).toFixed(2)}</span>
                        </div>
                        <div class="cart-item-controls">
                            <button class="qty-btn" data-action="decrease" data-id="${item.cartKey}">-</button>
                            <span class="cart-item-qty">${item.quantity}</span>
                            <button class="qty-btn" data-action="increase" data-id="${item.cartKey}">+</button>
                            <button class="remove-btn" data-id="${item.cartKey}"><i class="fas fa-trash"></i></button>
                        </div>
                    </div>
                `).join('');

                // Attach quantity/remove listeners
                cartItemsEl.querySelectorAll('.qty-btn').forEach(btn => {
                    btn.addEventListener('click', () => {
                        const id = btn.dataset.id;
                        const action = btn.dataset.action;
                        const item = cart.find(i => i.cartKey === id);
                        if (!item) return;
                        if (action === 'increase') item.quantity++;
                        if (action === 'decrease') {
                            item.quantity--;
                            if (item.quantity <= 0) cart = cart.filter(i => i.cartKey !== id);
                        }
                        updateCartUI();
                    });
                });

                cartItemsEl.querySelectorAll('.remove-btn').forEach(btn => {
                    btn.addEventListener('click', () => {
                        cart = cart.filter(i => i.cartKey !== btn.dataset.id);
                        updateCartUI();
                    });
                });
            }

            // Color swatch switching
            document.querySelectorAll('.color-swatch').forEach(swatch => {
                swatch.addEventListener('click', () => {
                    const card = swatch.closest('.product-card');
                    card.querySelectorAll('.color-swatch').forEach(s => s.classList.remove('active'));
                    swatch.classList.add('active');
                    card.querySelector('.selected-color-name').textContent = swatch.dataset.colorName;
                    const wrap = card.querySelector('.product-image-wrap');
                    if (wrap && wrap.classList.contains('has-slider')) {
                        const slides = wrap.querySelectorAll('.slide-img');
                        if (slides[0] && swatch.dataset.imgFront) slides[0].src = swatch.dataset.imgFront;
                        if (slides[1] && swatch.dataset.imgBack) slides[1].src = swatch.dataset.imgBack;
                    } else {
                        const img = card.querySelector('.product-img');
                        if (img) img.src = swatch.dataset.img;
                    }
                });
            });

            // Image sliders
            document.querySelectorAll('.product-image-wrap.has-slider').forEach(wrap => {
                const slides = wrap.querySelectorAll('.slide-img');
                const dots = wrap.querySelectorAll('.dot');
                let current = 0;

                function goTo(idx) {
                    slides[current].classList.remove('active');
                    dots[current].classList.remove('active');
                    current = idx;
                    slides[current].classList.add('active');
                    dots[current].classList.add('active');
                }

                wrap.querySelector('.prev').addEventListener('click', () => goTo(current === 0 ? slides.length - 1 : current - 1));
                wrap.querySelector('.next').addEventListener('click', () => goTo(current === slides.length - 1 ? 0 : current + 1));
                dots.forEach((dot, i) => dot.addEventListener('click', () => goTo(i)));
            });

            // Add to cart buttons
            document.querySelectorAll('.add-to-cart-btn').forEach(button => {
                button.addEventListener('click', () => {
                    const card = button.closest('.product-card');
                    const baseId = card && card.dataset.baseId;
                    const activeSwatch = card && card.querySelector('.color-swatch.active');
                    const fitSelect = card && card.querySelector('.fit-select');
                    const fit = fitSelect ? fitSelect.value : '';
                    const sizeSelect = card && card.querySelector('.size-select');
                    const size = sizeSelect ? sizeSelect.value : '';

                    if (fitSelect && !fit) {
                        fitSelect.classList.add('size-error');
                        fitSelect.focus();
                        return;
                    }
                    if (fitSelect) fitSelect.classList.remove('size-error');

                    if (sizeSelect && !size) {
                        sizeSelect.classList.add('size-error');
                        sizeSelect.focus();
                        return;
                    }
                    if (sizeSelect) sizeSelect.classList.remove('size-error');

                    let productId, name;
                    if (baseId && activeSwatch) {
                        const color = activeSwatch.dataset.color;
                        const colorName = activeSwatch.dataset.colorName;
                        productId = `${baseId}-${color}`;
                        name = `${button.dataset.productName} (${colorName})`;
                    } else {
                        productId = button.dataset.productId;
                        name = button.dataset.productName;
                    }

                    if (fit) name += ` \u2014 ${fit}`;
                    if (size) name += ` \u2014 Size ${size}`;

                    const price = button.dataset.productPrice;
                    const cartKey = `${productId}__${fit}__${size}`;

                    const existing = cart.find(i => i.cartKey === cartKey);
                    if (existing) {
                        existing.quantity++;
                    } else {
                        cart.push({ cartKey, productId, name, price, quantity: 1 });
                    }

                    updateCartUI();

                    // Quick feedback on the button
                    const original = button.innerHTML;
                    button.innerHTML = '<i class="fas fa-check"></i> Added!';
                    button.classList.add('added');
                    setTimeout(() => {
                        button.innerHTML = original;
                        button.classList.remove('added');
                    }, 1000);
                });
            });

            // Checkout
            checkoutBtn.addEventListener('click', async () => {
                if (cart.length === 0) return;

                checkoutBtn.disabled = true;
                checkoutBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Processing...';

                try {
                    const items = cart.map(item => ({
                        productId: item.productId,
                        displayName: item.name,
                        quantity: item.quantity,
                    }));

                    const response = await fetch('/api/create-checkout-session', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ items }),
                    });

                    const text = await response.text();
                    if (!text) {
                        throw new Error('Server returned an empty response (status ' + response.status + ').');
                    }

                    let data;
                    try {
                        data = JSON.parse(text);
                    } catch (parseErr) {
                        throw new Error('Server error (status ' + response.status + '): ' + text.substring(0, 200));
                    }

                    if (data.url) {
                        window.location.href = data.url;
                    } else {
                        throw new Error(data.error || 'Something went wrong');
                    }
                } catch (error) {
                    alert(error.message || 'Failed to start checkout. Please try again.');
                    checkoutBtn.disabled = false;
                    checkoutBtn.innerHTML = '<i class="fas fa-lock"></i> Checkout';
                }
            });

            // ========== STATUS BANNER ==========
            const params = new URLSearchParams(window.location.search);
            const status = params.get('status');
            const banner = document.getElementById('status-banner');

            if (status === 'success') {
                banner.textContent = 'Payment successful! You can collect your item at the next training or event.';
                banner.classList.remove('hidden');
                banner.classList.add('status-success');
                window.history.replaceState({}, '', '/shop');
            } else if (status === 'cancelled') {
                banner.textContent = 'Payment was cancelled. Feel free to try again!';
                banner.classList.remove('hidden');
                banner.classList.add('status-cancelled');
                window.history.replaceState({}, '', '/shop');
            }
        });
    </script>
</body>
</html>
